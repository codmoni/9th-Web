/** @type {import('tailwindcss').Config} */

module.exports = {
    theme: {
        extend: {
            colors: {
                'brand': '#ec4899', // 브랜드 컬러
                'layout-bg': '#3f3f46', // 레이아웃 영역 배경색
                'outlet-bg': '#18181b' // 아웃렛 영역 배경색
            }
        }
    }
}